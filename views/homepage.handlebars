{{!--
<!-- Loop over each object in the projects array -->
{{#each projects as |project| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/project/{{project.id}}">{{project.name}}</a>
    </h2>
    <p>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount project.needed_funding}}</span>
      needed!
    </p>
    <p>Created by {{project.user.name}} on {{format_date project.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div>
</div>
{{/each}} --}}
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Film Fan</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>

  <div class="jumbotron jumbotron-fluid justify-content-center" id="titleRow">
    <div class="container">
      <h1 class="display-4 mainHeader">Film Fan</h1>
      <p class="lead">Rotten Who???</p>
    </div>
  </div>

  <div class="container">
    <div class="row justify-content-center">
      <div class="col-8">

        <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
          <ol class="carousel-indicators">
            <li data-target="#carouselExampleIndicators" data-slide-to="0" class=""></li>
            <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
            <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
          </ol>
          <div class="carousel-inner">

            {{#each movies as | movie key | }}
            {{#if @first }}
            <div class="carousel-item active">
              {{else}}
              <div class="carousel-item">
                {{/if}}
                <img class="d-block w-100" src="{{movie.poster}}" alt="{{movie.title}} film poster">

                <div class="carousel-caption d-none d-md-block">
                  <h5>{{movie.title}}</h5>
                  <button type="button" class="btn btn-info">Add Review</button>
                </div>
              </div>
              {{/each}}
            </div>
          </div>

          <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
          </a>


          <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
          </a>

        </div>
      </div>
    </div>
  </div>


  <div class="container">
    <div class="row justify-content-center">
      <div class="col-8">

        {{!-- <form id="search-form" onsubmit="searchFormHandler(); return false;">
          <div class="form-group">
            <label for="exampleInputEmail1">Search by Title</label>
            <input type="text" class="form-control" id="search-result" placeholder="Enter Film">
          </div>

          <input type="submit" class="btn btn-primary login-form search">Submit</input>

        </form> --}}

      </div>

    </div>
    <form id="search-form">
      <div class="form-group">
        <label for="exampleInputEmail1">Search by Title</label>
        <input type="text" class="form-control" id="search-result" placeholder="Enter Film">
      </div>

      <button type="submit" id="search">Submit</button>

    </form>
  </div>

  {{!-- btn btn-primary login-form --}}


  <script src="/js/search.js" type="text/javascript"></script>

</body>

</html>